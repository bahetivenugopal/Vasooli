# Vasooli

Project instructions live in [`.claude/CLAUDE.md`](.claude/CLAUDE.md) — that file
is the single source of truth and the one to edit. This file exists only so the
instructions load from the repository root as well, whichever path the tool
resolves first. Keep it a pointer; never copy content here, or the two drift.

@.claude/CLAUDE.md
